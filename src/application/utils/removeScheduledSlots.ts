import { Slot } from "../interfaces/IPsychDetailsByUserUseCase";
import { isoToHHMM } from "../../utils/timeConverter";
import Session from "../../domain/entities/session.entity";

 
 export default function removeSheduledSlots(sessions:Session[],slots:Slot[]){
   
}